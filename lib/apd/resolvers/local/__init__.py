"""
Local resolvers try to map candidate participants to something in the event's corpus.
For example, the :class:`~apd.resolvers.local.token_resolver.TokenResolver` resolves candidates to tokens in the event's corpus.
"""
